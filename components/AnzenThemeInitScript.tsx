import { ANZEN_DARK, ANZEN_LIGHT } from "@/components/anzen-theme";

/** Runs before paint so the first frame matches stored light/dark preference. */
export function AnzenThemeInitScript() {
  const script = `(function(){try{var k="anzen-dark-mode";var v=localStorage.getItem(k);var dark=v===null?true:JSON.parse(v);var t=dark?${JSON.stringify(ANZEN_DARK)}:${JSON.stringify(ANZEN_LIGHT)};var r=document.documentElement;r.dataset.anzenTheme=dark?"dark":"light";r.classList.toggle("dark",dark);r.style.colorScheme=dark?"dark":"light";r.style.backgroundColor=t.bg;r.style.setProperty("--anzen-bg",t.bg);r.style.setProperty("--anzen-surface",t.surface);r.style.setProperty("--anzen-surface-2",t.surface2);r.style.setProperty("--anzen-border",t.border);r.style.setProperty("--anzen-text",t.text);r.style.setProperty("--anzen-text-light",t.textLight);r.style.setProperty("--anzen-muted",t.muted);r.style.setProperty("--anzen-caption",t.caption);r.style.setProperty("--anzen-subtle",t.subtle);r.style.setProperty("--anzen-accent",t.accent);r.style.setProperty("--anzen-accent-bg",t.accentBg);r.style.setProperty("--anzen-accent-text",t.accentText);r.style.setProperty("--background",t.bg);r.style.setProperty("--foreground",t.text);r.style.setProperty("--card",t.surface);r.style.setProperty("--card-foreground",t.text);r.style.setProperty("--popover",t.surface);r.style.setProperty("--popover-foreground",t.text);r.style.setProperty("--primary",t.accent);r.style.setProperty("--primary-foreground","#000000");r.style.setProperty("--secondary",t.surface2);r.style.setProperty("--secondary-foreground",t.text);r.style.setProperty("--muted",t.surface2);r.style.setProperty("--muted-foreground",t.muted);r.style.setProperty("--accent",t.accentBg);r.style.setProperty("--accent-foreground",t.accentText);r.style.setProperty("--border",t.border);r.style.setProperty("--input",t.border);r.style.setProperty("--ring",t.accent);if(document.body){document.body.style.backgroundColor=t.bg;document.body.style.color=t.text;}}catch(e){}})();`;

  return <script dangerouslySetInnerHTML={{ __html: script }} />;
}
